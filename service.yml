apiVersion: v1
kind: Service
metadata:
  name: orderbook-service
  namespace: qa
  labels:
    app: orderbook
spec:
  selector:
    app: orderbook
  ports:
    - port: 80
      targetPort: 8085
  type: LoadBalancer